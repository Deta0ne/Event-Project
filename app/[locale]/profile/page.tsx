export default async function page() {
    // // const req = await fetch('http://localhost:3000/api/search');
    // // const data = await req.json();
    // console.log(data);
    return <main>sadsa</main>;
}
